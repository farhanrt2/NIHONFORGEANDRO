<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.nihonforge" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Fullscreen imersif: sembunyikan status bar (sticky, swipe untuk intip).
             Layout no-limits + hide nav bar ditangani MainActivity (WindowInsets). -->
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>
